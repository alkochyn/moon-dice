{
  "name": "moon-dice",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Кубомет для Miro: формулы, пресеты, общая история бросков",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/check-bundle.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@mirohq/websdk-types": "^2.9.10",
    "@preact/preset-vite": "^2.9.1",
    "@types/node": "^26.5.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.2",
    "vitest": "^2.1.4"
  }
}
